__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

node_modules/
frontend/dist/
*.local

.env
.DS_Store

# Rendered by the backend from cluster.yml. Also ignored by central/.gitignore;
# listed here too so a local test run can't accidentally commit them.
central/targets/

# Prometheus target files, rendered by the backend into its working directory.
# Running the backend locally for testing drops them in the repo root — they
# belong to a deployment, not to the source, and have been committed by
# accident twice.
/agents.yml
/node-exporters.yml
